<head>
    <title>
        <%= title %>
    </title>
</head>

<body>
    <div id="index-container">
        <h1>Home Page</h1>
        <% if (currentUser) { %>
            <p>Welcome <%= currentUser.username %>
            </p>
            <% } else { %>
                <p>Please <a href="/login">Login</a></p>
                <% } %>
    </div>
</body>